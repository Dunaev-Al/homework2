user_string = input('Введите строку: ')
words_list = user_string.split()
for index in range(0, len(words_list)):
    if len(words_list[index]) >= 10:
        print((int(index) + 1), 'слово: ', words_list[index][:10])
    else:
        print((int(index) + 1), 'слово: ', words_list[index])
